{
  "name": "s3v-upload-handler",
  "version": "1.0.0",
  "description": "Lambda for S3 Vector to store files from S3 Bucket.",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --external:@aws-sdk/* --platform=node --target=es2020 --outdir=dist",
    "lint": "eslint ./src/* --ext .ts --fix"
  },
  "dependencies": {
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.138",
    "@types/node": "^20.14.9",
    "@types/uuid": "^10.0.0",
    "@typescript-eslint/eslint-plugin": "^8.43.0",
    "@typescript-eslint/parser": "^8.43.0",
    "esbuild": "^0.25.9",
    "eslint": "^9.35.0",
    "typescript": "^5.5.3",
    "typescript-eslint": "^8.43.0"
  }
}
