{
  "name": "rag-chatbot",
  "workspaces": [
    "infra",
    "ui"
  ],
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm run install:web && npm run install:infra && npm install",
    "install:infra": "npm install --workspace=infra",
    "install:web": "npm install --workspace=ui",
    "build:web": "npm run build --workspace=ui",
    "build:infra": "npm run cdk synth --workspace=infra",
    "build:layers": "npm run build --workspace=infra/lambda/layers/sample-just-in-time-layer",
    "build": "npm run build:web && npm run build:infra",
    "test": "npm test --workspace=infra",
    "dev": "npm run dev --workspace ui",
    "deploy": "npm run build && npm run deploy --workspace=infra",
    "deploy:local": "npm run build && npm run deploy:local --workspace=infra",
    "destroy": "npm run cdk destroy --workspace=infra"
  }
}
